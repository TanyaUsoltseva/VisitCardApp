export interface IOrder {
  name: string | null,
  address: string | null,
  telephone: number | null,
  mail: string | null,
  message: string | null,
  productId: string | null,
  userId: string | null,
  orderNumber: string | null,
  id?: string | null,
  _id?: string
}
